# ── Core ──────────────────────────────────────────────────────────────────────
numpy>=1.24
opencv-python>=4.7
imageio>=2.28          # EXR read/write — requires OpenEXR system library (see below)
Pillow>=9.0           # explicit LDR/WebP support for imageio/Pillow backends
scipy>=1.10

# ── ColorChecker detection ────────────────────────────────────────────────────
colour-checker-detection>=0.1.5
colour-science>=0.4.3  # pulled in by colour-checker-detection, listed for clarity

# ── YOLOv8 inference (optional but recommended for better swatch extraction) ──
ultralytics>=8.0       # downloads model (~88 MB) automatically on first run
click>=8.0             # required by colour-checker-detection CLI internals

# ── GUI (optional — only needed for hdri_cal_gui.py) ─────────────────────────
PySide6>=6.5

# ── OpenEXR system dependency (NOT a pip package) ────────────────────────────
# imageio uses OpenEXR under the hood.  Install the system library first:
#
#   Windows : pip install openexr-python   (or install OpenEXR via vcpkg)
#             OR use: pip install imageio[openexr]
#
#   macOS   : brew install openexr
#             pip install openexr
#
#   Linux   : sudo apt install libopenexr-dev
#             pip install openexr
#
# Alternatively imageio can use OpenCV as an EXR backend automatically
# if opencv-python is installed (which it is above) — no extra steps needed
# on most platforms.
